# SQLite database path, resolved relative to prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# Use a long random value for signing local sessions.
SESSION_SECRET="replace-with-at-least-32-random-characters"
